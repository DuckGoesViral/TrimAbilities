# Dispatch one iteration to the caller-chosen callback, forwarding the macro
# storage (which carries $(team) for the callback). Executor/position preserved.
$function $(cb) with storage trimabilities:iter
